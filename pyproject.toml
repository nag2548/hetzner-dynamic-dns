[project]
name = "hetzner-dynamic-dns"
version = "0.1.0"
description = "Update Hetzner DNS A records with the current public IP address."
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "black>=26.1.0",
]
